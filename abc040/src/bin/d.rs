#[allow(unused)]
use itertools::*;
use proconio::{marker::*, *};
#[allow(unused)]
use superslice::*;

fn main() {
    input! {
        n:usize,
        a:[usize;n],
        s:Chars,
    }
    let mut ans = 0;
    println!("{}", ans);
}
